# Django
SECRET_KEY=CAMBIA_ESTA_CLAVE_POR_UNA_LARGA_Y_ALEATORIA
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=
TIME_ZONE=America/Managua
ENABLE_HSTS=False

# PostgreSQL en producción. Déjalo vacío para SQLite local.
DATABASE_URL=

# =========================================================
# DEPÓSITOS: NOWPAYMENTS · SOLO USDT TRC20 Y BEP20
# =========================================================
# Genera la API key y el secreto IPN en tu cuenta NOWPayments. Nunca los
# publiques ni los pegues en campos visibles del panel HBL.
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=
NOWPAYMENTS_API_BASE_URL=https://api.nowpayments.io/v1
NOWPAYMENTS_TIMEOUT_SECONDS=15
NOWPAYMENTS_USER_AGENT=HBL-Payments/1.0 (+https://hbl-e8cw.onrender.com)

# Solo para pruebas temporales. En producción debe permanecer False.
NOWPAYMENTS_TEST_MODE=False
NOWPAYMENTS_TEST_MIN_USDT=1

# URL pública que registrarás como callback IPN. Déjala vacía en local para
# usar la URL calculada por Django. En producción debe ser HTTPS.
NOWPAYMENTS_IPN_CALLBACK_URL=https://hbl-e8cw.onrender.com/api/pagos/nowpayments/ipn/

# Los retiros USDT TRC20/BEP20 continúan bajo revisión y pago manual.

# Email opcional
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=HBL <no-reply@example.com>

# Media local (solo pruebas). En producción usa storage S3-compatible.
SERVE_MEDIA=True

# S3 / Cloudflare R2 / Wasabi / compatible (opcional)
AWS_STORAGE_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_REGION_NAME=
AWS_S3_ENDPOINT_URL=
AWS_S3_CUSTOM_DOMAIN=
AWS_QUERYSTRING_AUTH=False
